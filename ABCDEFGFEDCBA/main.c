#include <stdio.h>
#include <stdlib.h>

int main()
{int i, j;
    char alpha[] = {'A', 'B', 'C', 'D', 'E', 'F', 'G'};
    int number = 7;
    int num = number;

    for(i=1; i<=number; i++){
    for (j=0; j<num; j++){
    printf("%c", alpha[j]);
}
        for(sp=1))
num--;
printf("\n");
    }

    return 0;
}
