#include <stdio.h>
#include <stdlib.h>

int main()
{
    typedef enum Weekday {Sun,Mon, Tue=4, Wed, Thu, Fri, Sat};

    enum Weekday abc = Mon;

    if (3 - 3%5) {

    printf(" %d\n", abc);
}

    fun1();

    return 0;
}

int fun1()
{
    typedef int alwaro;
    alwaro num1= 4;
    printf(" %d", num1);
}
