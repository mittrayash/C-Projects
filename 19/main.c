#include <stdio.h>
#include <stdlib.h>

int main()
{char tuna;
    printf("Enter a singleton (digit/alphabet/alphanumeric): ");
    scanf(" %s", tuna);
    printf("\n");
    if ( isalpha (tuna) ){
    printf("%s is a alphabet.", tuna);}
else {printf("askdlsa";}
    return 0;
}
